class Customer extends migrosBase {
  constructor(firstName, lastName, customerCard, product) {
    super(firstName,lastName,customerCard,product);
  }
  calculate(){
   return super.calculate();
  }
}
